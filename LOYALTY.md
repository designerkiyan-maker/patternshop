# 🎁 باشگاه مشتریان (Loyalty System)

سیستم امتیاز وفاداری بات فروش الگو — منطبق بر معماری موجود (aiogram 3 + SQLite بدون ORM) و طبق اصول: دفتر کل غیرقابل‌تغییر، idempotency در سطح دیتابیس، منطق متمرکز خارج از هندلرها.

## معماری

```
هندلر تلگرام / Endpoint وب
        ↓
loyalty.py  (قواعد کسب‌وکار: نرخ‌ها، سطح‌ها، idempotency)
        ↓
database.py (عملیات اتمیک روی loyalty_state / loyalty_ledger)
        ↓
SQLite
```

## جداول

**loyalty_state** — وضعیت فعلی هر کاربر:
`user_id (PK, FK→users.telegram_id)`, `current_points` (CHECK ≥0), `lifetime_earned`, `lifetime_spent`, `tier`, `last_activity_at`

**loyalty_ledger** — دفتر کل غیرقابل‌تغییر (هر تغییر موجودی = یک رکورد):
`id`, `user_id (FK)`, `tx_type`, `amount` (CHECK ≠0), `balance_after` (CHECK ≥0), `reference_type`, `reference_id`, `idem_key`, `description`, `metadata`, `expires_at`, `created_at`

ایندکس‌ها: `uq_ledger_idem` (یکتای جزئی روی idem_key — قلب idempotency)، `user_id+id`، `created_at`، `(reference_type, reference_id)`.

انواع تراکنش: `PURCHASE`, `PURCHASE_REFUND`, `REGISTRATION_BONUS`, `REFERRAL_BONUS`, `CAMPAIGN_BONUS`, `TIER_BONUS`, `ADMIN_ADJUSTMENT`, `POINTS_REDEEM`, `POINTS_EXPIRE`, `REVERSAL`.

## قواعد کسب امتیاز

- **خرید:** فقط پس از تایید واقعی سفارش (status=approved) — در هر ۴ مسیر تایید (بات، خرید آنی با کیف پول، مینی‌اپ، پنل وب): `امتیاز = ⌊مبلغ نهایی پرداختی ÷ نرخ⌋ × ضریب سطح فعلی` (با سقف اختیاری هر سفارش).
- **برگشت:** ردِ سفارش، امتیاز همان سفارش را برمی‌گرداند (رکورد جدا؛ رکورد اصلی دست نمی‌خورد؛ دوباره‌برگشت غیرممکن).
- **ثبت‌نام:** هدیه‌ی یک‌بار برای هر کاربر (اولین /start یا اولین ورود به مینی‌اپ).
- **معرفی:** به دعوت‌کننده وقتی نفر جدید واقعاً از لینک دعوت وارد می‌شود (یک‌بار به ازای هر جفت).
- **پرداخت ناموفق/ردشده:** صفر امتیاز.

## سطح‌ها (Tier)

بر اساس **کل امتیازهای کسب‌شده از ابتدا** (`lifetime_earned`) — قطعی و قابل تست. ضریبِ «سطح فعلی» مشتری در لحظه‌ی خرید اعمال می‌شود؛ عبور از آستانه روی خریدهای بعدی اثر می‌گذارد و برگشت خرید، سطح را پایین نمی‌آورد.

## تبدیل به کیف پول

جریان تراکنشی امن در یک تراکنش SQLite: کاهش شرطی امتیاز (`WHERE current_points >= ?` → موجودی منفی غیرممکن) + درج دفتر کل + افزایش `referral_credit`. اگر هر گام شکست بخورد کل تراکنش rollback می‌شود.

## Idempotency

هر رویداد یک `idem_key` یکتا دارد: `purchase:{order_id}`, `refund:{order_id}`, `registration:{tg_id}`, `referral:{referrer}:{referred}` — ایندکس یکتای دیتابیس حتی در برابر callback تکراری، وب‌هوک دوباره، یا درخواست هم‌زمان، اعطای دوباره را غیرممکن می‌کند.

## تنظیمات (پنل ادمین بات ← ⭐ باشگاه مشتریان)

| کلید | پیش‌فرض | توضیح |
|---|---|---|
| `loyalty_enabled` | 1 | فعال/غیرفعال کل سیستم (+ نمایش دکمه‌ی منو) |
| `loyalty_points_per_toman` | 10000 | هر امتیاز = این تعداد تومان خرید |
| `loyalty_reg_bonus` | 50 | هدیه ثبت‌نام (0=خاموش) |
| `loyalty_referral_bonus` | 20 | امتیاز معرفی (0=خاموش) |
| `loyalty_redeem_points` | 100 | مخرج تبدیل |
| `loyalty_redeem_toman` | 10000 | مبلغ تومانی هر تبدیل |
| `loyalty_min_redeem` | 100 | حداقل تبدیل |
| `loyalty_max_per_order` | 0 | سقف امتیاز هر سفارش (0=نامحدود) |
| `loyalty_tiers` | برنز/نقره/طلا/پلاتین | JSON؛ فرمت ویرایش: `نام:آستانه:ضریب%` با کاما |

## تست‌ها

```bash
pip install -r requirements-dev.txt
python -m pytest tests/ -q
```
۳۵ تست: خرید (نرخ، مبلغ نهایی، سقف، غیرفعال)، برگشت (ساده/تکراری/بدون اعطا/اثر روی سطح)، ثبت‌نام، معرفی، تبدیل (معتبر/ناکافی/صفر/منفی/زیر حداقل/غیرمضرب/**هم‌زمان بین دو پروسه**)، سطح‌ها (مرز دقیق، ضریب، سطوح سفارشی)، دفتر کل (balance_after، صفحه‌بندی، تغییرناپذیری، idempotency)، امنیت (تعدیل بدون دلیل/صفر/منفیِ فراتر از موجودی).

## مهاجرت

هیچ مهاجرت دستی لازم نیست — جداول جدید با `CREATE TABLE IF NOT EXISTS` در اولین اجرای بات/مینی‌اپ/پنل ساخته می‌شوند و داده‌ی موجود دست‌نخورده می‌ماند.

## نقاط توسعه‌ی آینده

- **انقضای امتیاز:** ستون `expires_at` و طراحی دفتر کل آماده است؛ فقط یک جاب پس‌زمینه (مثل backup_loop) باید `loyalty.expire_due` را صدا بزند (در v1 عمداً NotImplementedError است).
- **مینی‌اپ:** فیلدهای loyalty در `/api/me` موجود است؛ نمایش UI فرانت‌اندی بعداً.

## پنل مدیریت وب

همه‌چیز در پنل وب هم هست (هم‌ترازی کامل با بات):
- **تنظیمات فروش ← ⭐ باشگاه مشتریان:** کارت کامل تنظیمات (فعال/غیرفعال، نرخ، هدیه‌ها، نرخ تبدیل، حداقل، سقف، سطوح) → `GET/POST /api/settings/loyalty`
- **جزئیات هر کاربر:** کارت امتیاز + سطح + فرم تعدیل با دلیل → `POST /api/users/{tg_id}/loyalty`
- **مدیریت ادمین‌های تلگرامی** (فقط مالک): لیست/افزودن/تغییر نقش/حذف → `/api/telegram-admins`
- مستندات کامل: [LOYALTY.md](LOYALTY.md)
